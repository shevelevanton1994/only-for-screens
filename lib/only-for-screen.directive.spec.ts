import { OnlyForScreenDirective } from './only-for-screen.directive';

describe('OnlyForScreenDirective', () => {
    it('should create an instance', () => {
        const directive = new OnlyForScreenDirective();
        expect(directive).toBeTruthy();
    });
});
